columns:
    contactList:
        label: Lista
        relation: contactList
        select: name
        default: "Todas"

    offset_label:
        label: Configuración
        type: text
        sortable: false

    active:
        label: Activa
        type: switch
        width: 100px

    created_at:
        label: Creada
        type: datetime
        width: 160px
