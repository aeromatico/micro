# ==================================================================
# Form config for MetaAccounts controller.
# Reuses the field definitions co-located with the MetaAccount model.
# ==================================================================
name: aero.masterads::lang.nav.meta_accounts
form: $/aero/masterads/models/metaaccount/fields.yaml
modelClass: Aero\MasterAds\Models\MetaAccount
defaultRedirect: aero/masterads/metaaccounts

create:
    title: Conectar Cuenta Meta
    redirect: aero/masterads/metaaccounts/update/:id

update:
    title: Cuenta Meta
    redirect: aero/masterads/metaaccounts

preview:
    title: Cuenta Meta
